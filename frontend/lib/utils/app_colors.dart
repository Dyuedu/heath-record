import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Primary teal color - used for FAB, icons, accents
  static const Color primaryTeal = Color(0xFF75DDD5);

  /// Blue color - used for appointment date/time
  static const Color appointmentBlue = Color(0xFF3F9AEF);

  /// Light teal background - used for feature item circles
  static const Color lightTealBg = Color(0xFFE8F6F5);

  /// Border grey - used for dividers and borders
  static const Color borderGrey = Color(0xFFE0E0E0);
}
