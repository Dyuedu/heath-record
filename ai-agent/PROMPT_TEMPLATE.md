# Prompt Template

When implementing a feature:

1. Create backend API in Spring Boot
2. Create Flutter model matching the API response
3. Implement ViewModel method calling the API
4. Connect View with ViewModel

Ensure field mapping between:

Spring DTO
↓
JSON
↓
Flutter Model