# Rules

Backend

- Use Controller → Service → Repository architecture.
- Use DTO for request and response.
- Return JSON responses.

Frontend

- Use MVVM architecture.
- View must not contain business logic.
- ViewModel handles API calls.

Mapping

- Flutter model must match backend response JSON.
- Use the same field names between DTO and Flutter model.